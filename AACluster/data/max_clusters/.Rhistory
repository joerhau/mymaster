help(hclust)
quit()
library(cluster)
tbl<-read.table(commandArgs()[4], head=TRUE)
#create distance matrix
d<-as.dist(tbl)
#cluster
clust<-hclust(d,method="complete")
#plot cluster dendrogramm
plot(clust)
clust
tbl<-read.table("max.txt", head=TRUE)
d<-as.dist(tbl)
#cluster
clust<-hclust(d,method="complete")
#plot cluster dendrogramm
plot(clust)
silhouette(cutree(clust,4),d)
cutree(clust,4)
silhouette
silhouette()
mean
silhouette ?
cluster.silhouette
silhouette
plot
silhouette
cluster:silhouette
help(silhouette)
function
help(function)
help(silhouette)
help(function)
getAnywhere(silhouette)
getAnywhere(silhouette)[1]
getAnywhere(silhouette)[2]
?::silhouette
?silhouette
?cluster::silhouette
getAnywhere("silhouette)[2]
getAnywhere("silhouette")[2]
getAnywhere("silhouette")
help(UseMethod)
silhouette(cutree(clust,4))
silhouette()
silhouette
silhouette(cutree(clust,4),d)
help(cbind)
quit()
cutree(clust,4)
cutree(clust,5)
help(write)
write(cutree(clust,4),"tmp.txt")
cutree(clust,4)
help(write.table)
write.table(cutree(clust,4),"tmp.txt")
write.csv(cutree(clust,4),"tmp.txt")
write.ftable(cutree(clust,4),"tmp.txt")
write.dcf(cutree(clust,4),"tmp.txt")
write(cutree(clust,4),"tmp.txt")
help(write)
help(write)
write(cutree(clust,4),"tmp.txt",ncolumns=20)
quit()
